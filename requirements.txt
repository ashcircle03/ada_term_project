# ─────────────────────────────────────────────────────────────
# 실행 환경: .conda (Python 3.11)
#
# 최초 설정:
#   conda create -p .conda python=3.11 -y
#   pip install -r requirements.txt
#   python -m ipykernel install --user --name ada --display-name "ada (Python 3.11)"
#
# 이후 노트북 커널: "ada (Python 3.11)" 선택
# ─────────────────────────────────────────────────────────────

# ── 크롤러 ────────────────────────────────────────────────────
requests==2.34.0
beautifulsoup4==4.14.3
lxml==6.1.0

# ── 데이터·분석 ───────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0
statsmodels==0.14.6
pyarrow==24.0.0

# ── 시각화 ────────────────────────────────────────────────────
matplotlib==3.10.9

# ── 지도학습 (H3) ─────────────────────────────────────────────
# macOS: xgboost가 OpenMP를 요구함 → brew install libomp 선행 필요
xgboost==3.2.0

# ── 클러스터링 보조 (H1) ──────────────────────────────────────
hdbscan==0.8.43

# ── 사후검정 보조 (H2) ────────────────────────────────────────
scikit-posthocs==0.13.0

# ── Jupyter ──────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==7.2.0
